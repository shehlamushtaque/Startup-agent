# Pinned to the versions this project is known to run on (Python 3.13).
# Install with:  pip install -r requirements.txt

# Web framework (API)
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
websockets==17.1

# Data
pandas==2.3.3
numpy==2.3.4
pyarrow==22.0.0

# Retrieval / embeddings (Q&A index)
# Pulls in torch + transformers transitively; expect a large download.
sentence-transformers==5.1.2
faiss-cpu==1.12.0
scikit-learn==1.7.2

# HTTP clients & LLM SDK
requests==2.32.5
httpx==0.28.1
openai==2.8.0

# Utilities
python-dotenv==1.2.1

# --- Dev / testing (optional) ---------------------------------------------
# Not required to run the app; install if you want to run the test suite.
# pytest==7.4.3
# pytest-asyncio==0.21.1
